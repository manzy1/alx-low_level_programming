#include "main.h"

/**
 * main - print fizzbuzz
 * Return: Always 0
 */
int main(void)
{
	int n;

	printf("1");

	for (n = 2; n < 100; n++)
	{
		printf(" ");
		if (n % 3 == 0)
			print("fizz");
		if (n % 5 == 00
				print("buzz"0
		if (n % 3 != 0 && n % 5 != 0)
		print("%d%", n);
	}

	printf('\n');
	return (0);
}
